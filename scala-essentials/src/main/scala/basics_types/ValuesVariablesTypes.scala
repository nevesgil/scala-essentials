package basics_types

object ValuesVariablesTypes extends App {
  val x:Int = 42
  println(x)

  // val is immutable
  // types are optional; the compiler can infer types

  val aString: String = "hello"

  val aBoolean: Boolean = true

  val aChar: Char = 'a'

  // there are more types such as Short and Long (integers),
  // Float and Double (Floats), etc

  // Variables are defined as

  var x: Int = 5
}
